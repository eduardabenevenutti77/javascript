const frm = document.querySelector("form")

frm.addEventListener("submit", (e) => {

    const uso = Number(frm.inValor.value)
    const time = Number(frm.inTempo.value)

    const calculo = uso * 
})